# postgres:16-alpine only.
#
# CVE-2025-68121 (Go crypto/tls stdlib): CRITICAL with upstream fixes
# released (Go 1.24.13) but the pinned alpine postgres digest predates
# the rebuild (reviewed again 2026-09-16 — the digest is immutable, so
# this clears only when Dependabot bumps the pin to a rebuilt image).
# The Go binary involved is build tooling, not the postgres server
# itself; the container is on the internal network with no TLS ingress.
# Revisit: drop when the 16-alpine pin in docker-compose.prod.yml moves
# to a digest carrying Go >= 1.24.13.
CVE-2025-68121
